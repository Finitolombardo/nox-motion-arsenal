import { useMemo, type CSSProperties } from 'react';
import { seededRandom } from '../../lib/animationUtils';
export interface MorphingBlobFieldProps { blobs?: number; color?: string; }
export default function MorphingBlobField({ blobs = 4, color = '#d4a24a' }: MorphingBlobFieldProps) { const count = Math.max(3, Math.min(6, Math.round(blobs))); const data = useMemo(() => { const r = seededRandom(71); return Array.from({ length: count }, (_, i) => ({ i, x: r() * 85, y: r() * 80, size: 18 + r() * 25, duration: 8 + r() * 9, delay: -r() * 8 })); }, [count]); return <div className="nox-blobs" style={{ '--blob-gold': color } as CSSProperties}>{data.map(b => <i key={b.i} style={{ '--x': `${b.x}%`, '--y': `${b.y}%`, '--size': `${b.size}%`, '--duration': `${b.duration}s`, '--delay': `${b.delay}s` } as CSSProperties}/>)}<style>{CSS}</style></div>; }
const CSS = `.nox-blobs{position:relative;min-height:280px;overflow:hidden;background:#08090c;isolation:isolate}.nox-blobs i{position:absolute;left:var(--x);top:var(--y);width:var(--size);aspect-ratio:1;background:radial-gradient(circle at 35% 30%,#f7e8a4aa,#d4a24a66 32%,transparent 72%);filter:blur(18px);mix-blend-mode:screen;animation:blob-morph var(--duration) ease-in-out var(--delay) infinite alternate}.nox-blobs i:nth-child(2n){background:radial-gradient(circle,#734719aa,transparent 70%);animation-direction:alternate-reverse}@keyframes blob-morph{0%{border-radius:41% 59% 41% 59% / 53% 51% 49% 47%;transform:rotate(-12deg) scale(.9)}50%{border-radius:69% 31% 56% 44% / 35% 62% 38% 65%;transform:rotate(28deg) scale(1.15)}100%{border-radius:34% 66% 72% 28% / 62% 34% 66% 38%;transform:rotate(70deg) scale(.98)}}@media(prefers-reduced-motion:reduce){.nox-blobs i{animation:none}}`;
