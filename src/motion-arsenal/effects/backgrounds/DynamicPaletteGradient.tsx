import { useMemo, type CSSProperties } from 'react';
export interface DynamicPaletteGradientProps { colors?: string[]; angle?: number; speed?: number; color?: string; }
export default function DynamicPaletteGradient({ colors = ['#d4a24a', '#5b3517', '#11100d'], angle = 120, speed = 18, color = '#d4a24a' }: DynamicPaletteGradientProps) {
  const palette = useMemo(() => colors.filter(Boolean).slice(0, 6), [colors]);
  const style = { '--palette': palette.join(', '), '--angle': `${angle}deg`, '--flow': `${Math.max(.5, speed)}s`, '--accent': color } as CSSProperties;
  return <div className="nox-palette" style={style} role="img" aria-label="Dynamic palette gradient"><div className="nox-palette__mesh" /><span className="nox-palette__label">DYNAMIC / PALETTE</span><style>{`.nox-palette{position:relative;min-height:220px;overflow:hidden;background:#090807;color:var(--accent);isolation:isolate}.nox-palette__mesh{position:absolute;inset:-25%;background:linear-gradient(var(--angle),var(--palette));background-size:220% 220%;filter:saturate(1.2);animation:palette-flow var(--flow) ease-in-out infinite alternate}.nox-palette__mesh:after{content:"";position:absolute;inset:0;background:radial-gradient(circle at 20% 30%,#fff4,transparent 25%),radial-gradient(circle at 80% 70%,#0007,transparent 30%);mix-blend-mode:soft-light}.nox-palette__label{position:absolute;left:22px;bottom:18px;font:700 .7rem ui-monospace;letter-spacing:.18em;text-shadow:0 0 12px #000}@keyframes palette-flow{to{transform:translate3d(5%,-4%,0) scale(1.08);background-position:100% 0}}@media(prefers-reduced-motion:reduce){.nox-palette__mesh{animation:none}}`}</style></div>;
}
