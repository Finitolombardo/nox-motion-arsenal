import { useState, type CSSProperties } from 'react';
export interface RadarSweepGlitchButtonProps { label?: string; color?: string; }
export default function RadarSweepGlitchButton({ label = 'SCAN TARGET', color = '#d4a24a' }: RadarSweepGlitchButtonProps) { const [active, setActive] = useState(false); return <button className={`nox-radar-btn ${active ? 'is-active' : ''}`} onClick={() => setActive(v => !v)} style={{ '--radar-gold': color } as CSSProperties}><span className="scan"/><b data-text={label}>{label}</b><i>{active ? 'SIGNAL LOCKED' : 'READY / SWEEP'}</i><style>{CSS}</style></button>; }
const CSS = `.nox-radar-btn{position:relative;display:grid;gap:8px;min-width:220px;padding:22px 28px;border:1px solid var(--radar-gold);background:#0c0d0f;color:#f7e8a4;overflow:hidden;cursor:pointer;text-align:left}.nox-radar-btn .scan{position:absolute;inset:0;background:linear-gradient(90deg,transparent,#d4a24a55,transparent);transform:translateX(-120%);animation:radar-sweep 2.3s steps(9,end) infinite}.nox-radar-btn b{position:relative;font:700 1.1rem ui-monospace;letter-spacing:.12em}.nox-radar-btn b:after{content:attr(data-text);position:absolute;inset:0;color:#39d5d5;transform:translate(2px,-1px);opacity:0}.nox-radar-btn.is-active b:after{opacity:.7;animation:radar-glitch .35s steps(2) 3}.nox-radar-btn i{font:9px ui-monospace;letter-spacing:.2em;opacity:.65}.nox-radar-btn:focus-visible{outline:2px solid var(--radar-gold);outline-offset:5px}@keyframes radar-sweep{to{transform:translateX(120%)}}@keyframes radar-glitch{50%{transform:translate(-3px,2px);color:#ff4b62}}@media(prefers-reduced-motion:reduce){.nox-radar-btn .scan{animation:none;transform:none}.nox-radar-btn b:after{animation:none}}`;
