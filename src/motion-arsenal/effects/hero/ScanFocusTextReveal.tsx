import { useState, type CSSProperties } from 'react';
export interface ScanFocusTextRevealProps { text?: string; color?: string; }
export default function ScanFocusTextReveal({ text = 'FOCUS THE SIGNAL', color = '#d4a24a' }: ScanFocusTextRevealProps) { const [active, setActive] = useState(false); return <button className={`nox-scan-text ${active ? 'is-scanned' : ''}`} onMouseEnter={() => setActive(true)} onMouseLeave={() => setActive(false)} onFocus={() => setActive(true)} onClick={() => setActive(v => !v)} style={{ '--scan-gold': color } as CSSProperties}><span>{text}</span><i/><small>{active ? 'VIEWFINDER / LOCKED' : 'HOVER TO SCAN'}</small><style>{CSS}</style></button>; }
const CSS = `.nox-scan-text{position:relative;display:grid;gap:18px;width:100%;min-height:220px;border:0;background:#090a0d;color:#f7e8a4;cursor:pointer;overflow:hidden}.nox-scan-text span{font-size:clamp(1.6rem,7vw,4.8rem);font-weight:800;letter-spacing:.05em;filter:blur(8px);opacity:.5;transition:filter .8s,opacity .8s}.nox-scan-text.is-scanned span{filter:blur(0);opacity:1}.nox-scan-text i{position:absolute;top:0;bottom:0;left:0;width:2px;background:var(--scan-gold);box-shadow:0 0 18px 4px var(--scan-gold);transform:translateX(0);transition:transform 1s cubic-bezier(.2,1,.3,1)}.nox-scan-text.is-scanned i{transform:translateX(100vw)}.nox-scan-text small{font:10px ui-monospace;letter-spacing:.18em;opacity:.6}.nox-scan-text:focus-visible{outline:2px solid var(--scan-gold);outline-offset:5px}@media(prefers-reduced-motion:reduce){.nox-scan-text span{filter:none;opacity:1}.nox-scan-text i{display:none}.nox-scan-text *{transition:none}}`;
