import { useState, type CSSProperties } from 'react';
export interface LabyrinthBalanceToggleProps { initial?: boolean; color?: string; }
export default function LabyrinthBalanceToggle({ initial = false, color = '#d4a24a' }: LabyrinthBalanceToggleProps) {
  const [on, setOn] = useState(initial); const [tilt, setTilt] = useState({ x: 0, y: 0 });
  return <button className={`nox-labyrinth ${on ? 'is-on' : ''}`} onClick={() => setOn(v => !v)} onPointerMove={e => { const r = e.currentTarget.getBoundingClientRect(); setTilt({ x: (e.clientY - r.top / 2) / r.height * 8, y: (e.clientX - r.left / 2) / r.width * -8 }); }} onPointerLeave={() => setTilt({ x: 0, y: 0 })} aria-pressed={on} style={{ '--labyrinth-gold': color, '--tilt-x': `${tilt.x}deg`, '--tilt-y': `${tilt.y}deg` } as CSSProperties}><span className="nox-labyrinth__maze"><i className="ball"/><b className="goal">ON</b><em className="wall wall-a"/><em className="wall wall-b"/><em className="wall wall-c"/><em className="wall wall-d"/></span><strong>{on ? 'BALANCED · ON' : 'BALANCE TO ACTIVATE'}</strong><style>{CSS}</style></button>;
}
const CSS = `.nox-labyrinth{display:grid;place-items:center;gap:20px;width:100%;min-height:300px;border:1px solid #d4a24a55;background:#090a0d;color:#f7e8a4;cursor:pointer}.nox-labyrinth__maze{position:relative;width:min(230px,60vw);aspect-ratio:1;border:10px solid #3a2716;border-radius:16px;background:repeating-linear-gradient(0deg,transparent 0 28px,#d4a24a45 29px 31px),repeating-linear-gradient(90deg,transparent 0 28px,#d4a24a45 29px 31px);transform:perspective(400px) rotateX(var(--tilt-x)) rotateY(var(--tilt-y));transition:transform .35s;box-shadow:inset 0 0 30px #000,0 0 18px #d4a24a33}.wall{position:absolute;background:#d4a24a;height:6px;border-radius:3px;box-shadow:0 0 8px #d4a24a}.wall-a{left:12%;top:30%;width:56%;transform:rotate(30deg)}.wall-b{left:30%;top:55%;width:60%;transform:rotate(-24deg)}.wall-c{left:15%;top:70%;width:38%;transform:rotate(12deg)}.wall-d{left:64%;top:18%;width:25%;transform:rotate(72deg)}.ball{position:absolute;z-index:2;left:18%;top:18%;width:20px;height:20px;border-radius:50%;background:#f7e8a4;box-shadow:0 0 16px #f7e8a4;transition:all .75s cubic-bezier(.2,1.5,.35,1)}.is-on .ball{left:75%;top:74%}.goal{position:absolute;right:8%;bottom:7%;font:10px ui-monospace;color:#d4a24a}.nox-labyrinth strong{font:11px ui-monospace;letter-spacing:.18em}.nox-labyrinth:focus-visible{outline:2px solid var(--labyrinth-gold);outline-offset:5px}@media(prefers-reduced-motion:reduce){.nox-labyrinth__maze,.nox-labyrinth .ball{transition:none}}`;
