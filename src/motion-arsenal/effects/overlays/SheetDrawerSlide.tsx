import { useState, type CSSProperties } from 'react';
export interface SheetDrawerSlideProps { title?: string; color?: string; }
export default function SheetDrawerSlide({ title = 'COMMAND SHEET', color = '#d4a24a' }: SheetDrawerSlideProps) { const [open,setOpen]=useState(false); return <div className="nox-sheet-wrap" style={{ '--sheet-gold': color } as CSSProperties}><button className="open" onClick={()=>setOpen(true)}>{title} <span>↑</span></button>{open&&<div className="nox-sheet-backdrop" onClick={()=>setOpen(false)}><section className="nox-sheet" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><button className="handle" onClick={()=>setOpen(false)} aria-label="Close drawer"/><strong>{title}</strong><p>Snap the sheet upward to inspect a focused layer of the interface.</p><button onClick={()=>setOpen(false)}>DISMISS ↘</button></section></div>}<style>{CSS}</style></div>; }
const CSS = `.nox-sheet-wrap{min-height:220px;display:grid;place-items:center;background:#090a0d}.nox-sheet-wrap .open,.nox-sheet button{border:1px solid var(--sheet-gold);background:#15100c;color:#f7e8a4;padding:14px 20px;font:10px ui-monospace;letter-spacing:.16em;cursor:pointer}.nox-sheet-wrap .open span{color:var(--sheet-gold);font-size:18px}.nox-sheet-backdrop{position:fixed;z-index:30;inset:0;display:flex;align-items:flex-end;background:#0008;backdrop-filter:blur(5px);animation:sheet-fade .3s}.nox-sheet{width:100%;min-height:34vh;padding:30px max(24px,calc((100vw - 760px)/2));background:linear-gradient(#17120c,#090a0d);border-top:1px solid var(--sheet-gold);box-shadow:0 -20px 60px #000;animation:sheet-up .55s cubic-bezier(.2,1.3,.3,1)}.nox-sheet .handle{display:block;width:70px;height:5px;margin:0 auto 30px;border:0;border-radius:9px;background:var(--sheet-gold);padding:0}.nox-sheet strong{font-size:clamp(1.3rem,4vw,2.5rem);color:#f7e8a4}.nox-sheet p{color:#c6b58f;max-width:450px;line-height:1.6}.nox-sheet button:last-child{margin-top:18px}@keyframes sheet-up{from{transform:translateY(100%)}to{transform:none}}@keyframes sheet-fade{from{opacity:0}to{opacity:1}}@media(prefers-reduced-motion:reduce){.nox-sheet-backdrop,.nox-sheet{animation:none}}`;
