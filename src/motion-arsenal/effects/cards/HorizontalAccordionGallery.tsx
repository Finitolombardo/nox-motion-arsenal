import { useState, type CSSProperties } from 'react';
export interface HorizontalAccordionGalleryProps { images?: string[]; labels?: string[]; color?: string; }
const FALLBACK = ['ORIGIN','SIGNAL','MOTION','NIGHT'];
export default function HorizontalAccordionGallery({ images = [], labels = FALLBACK, color = '#d4a24a' }: HorizontalAccordionGalleryProps) {
  const [active, setActive] = useState(0); const items = labels.length ? labels : FALLBACK;
  return <section className="nox-haccordion" style={{ '--accordion-gold': color } as CSSProperties} aria-label="Motion gallery">{items.map((label, i) => <button key={label + i} className={i === active ? 'is-active' : ''} onMouseEnter={() => setActive(i)} onFocus={() => setActive(i)} onClick={() => setActive(i)} style={{ '--panel-image': `url(${images[i] || ''})` } as CSSProperties}><span className="nox-haccordion__wash"/><span className="nox-haccordion__index">0{i + 1}</span><strong>{label}</strong><small>Explore sequence <b>↗</b></small></button>)}<style>{CSS}</style></section>;
}
const CSS = `.nox-haccordion{display:flex;min-height:300px;background:#090a0d;gap:2px;padding:8px;overflow:hidden}.nox-haccordion button{position:relative;isolation:isolate;flex:1;min-width:0;border:0;border-left:1px solid #d4a24a66;background:linear-gradient(150deg,#261a10,#0d0d0e);color:#f7e8a4;text-align:left;padding:22px;cursor:pointer;transition:flex-grow .65s cubic-bezier(.2,1,.3,1),filter .65s}.nox-haccordion button.is-active{flex-grow:4;filter:saturate(1.2)}.nox-haccordion__wash{position:absolute;inset:0;z-index:-1;background-image:linear-gradient(90deg,#08090dcc,#08090d55),var(--panel-image);background-size:cover;background-position:center;opacity:.2;transition:opacity .5s,transform .8s}.nox-haccordion button.is-active .nox-haccordion__wash{opacity:.72;transform:scale(1.05)}.nox-haccordion__index{font:11px ui-monospace;opacity:.7}.nox-haccordion strong{position:absolute;bottom:62px;left:22px;font-size:clamp(1rem,2.8vw,2rem);letter-spacing:.12em;writing-mode:vertical-rl;transform:rotate(180deg);transition:writing-mode .3s}.nox-haccordion button.is-active strong{writing-mode:horizontal-tb;transform:none}.nox-haccordion small{position:absolute;bottom:22px;left:22px;opacity:0;transform:translateY(8px);transition:.35s}.nox-haccordion button.is-active small{opacity:1;transform:none}.nox-haccordion button:focus-visible{outline:2px solid var(--accordion-gold);outline-offset:-5px}@media(max-width:600px){.nox-haccordion{min-height:360px}.nox-haccordion strong{font-size:.85rem}.nox-haccordion button{padding:12px}.nox-haccordion button.is-active{flex-grow:5}}@media(prefers-reduced-motion:reduce){.nox-haccordion *{transition:none!important}}`;
