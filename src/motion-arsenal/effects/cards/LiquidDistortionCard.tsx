import { useState, type CSSProperties } from 'react';
export interface LiquidDistortionCardProps { title?: string; body?: string; color?: string; }
export default function LiquidDistortionCard({ title = 'LIQUID SIGNAL', body = 'Hover the surface to wake the fluid field.', color = '#d4a24a' }: LiquidDistortionCardProps) {
  const [hover, setHover] = useState(false); const [pressed, setPressed] = useState(false);
  return <button className={`nox-liquid-card ${hover ? 'is-wet' : ''} ${pressed ? 'is-pressed' : ''}`} onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)} onFocus={() => setHover(true)} onBlur={() => setHover(false)} onClick={() => setPressed(v => !v)} style={{ '--liquid-gold': color } as CSSProperties}><svg width="0" height="0" aria-hidden="true"><filter id="nox-liquid-filter"><feTurbulence type="fractalNoise" baseFrequency=".018 .05" numOctaves="2" seed="8"><animate attributeName="baseFrequency" values=".018 .05;.04 .015;.018 .05" dur="5s" repeatCount="indefinite"/></feTurbulence><feDisplacementMap in="SourceGraphic" scale={hover ? 18 : 3}/></filter></svg><span className="nox-liquid-card__orb"/><small>NOX / FLUID STUDY</small><strong>{title}</strong><p>{body}</p><i>{pressed ? 'FIELD STABILIZED' : 'MOVE TO DISTORT'} <b>↗</b></i><style>{CSS}</style></button>;
}
const CSS = `.nox-liquid-card{position:relative;isolation:isolate;display:grid;align-content:end;gap:12px;min-height:270px;width:100%;padding:28px;text-align:left;border:1px solid #d4a24a55;border-radius:18px;overflow:hidden;background:linear-gradient(135deg,#21160d,#0b0c0f 58%);color:#f7e8a4;cursor:pointer;filter:url(#nox-liquid-filter);transition:box-shadow .5s,transform .5s}.nox-liquid-card.is-wet{box-shadow:0 0 35px #d4a24a44,inset 0 0 30px #d4a24a14;transform:translateY(-4px)}.nox-liquid-card__orb{position:absolute;z-index:-1;right:-10%;top:-20%;width:65%;aspect-ratio:1;border-radius:46% 54% 62% 38% / 38% 42% 58% 62%;background:radial-gradient(circle at 30% 30%,#f7e8a499,#d4a24a33 35%,transparent 70%);filter:blur(8px);animation:liquid-morph 8s ease-in-out infinite alternate}.nox-liquid-card small,.nox-liquid-card i{font:10px ui-monospace;letter-spacing:.18em;opacity:.7}.nox-liquid-card strong{font-size:clamp(1.3rem,4vw,2.2rem)}.nox-liquid-card p{max-width:320px;margin:0;color:#d9cda9}.nox-liquid-card i{font-style:normal}.nox-liquid-card b{color:var(--liquid-gold);font-size:18px}.nox-liquid-card:focus-visible{outline:2px solid var(--liquid-gold);outline-offset:5px}@keyframes liquid-morph{to{border-radius:62% 38% 34% 66% / 54% 64% 36% 46%;transform:rotate(24deg) scale(1.08)}}@media(prefers-reduced-motion:reduce){.nox-liquid-card__orb{animation:none}.nox-liquid-card{transition:none}}`;
